// ./src/FormRange.tsx/Presets/0-default.jsx
import React from 'react';
import FormRange from '.././src/FormRange';

export default (
  <FormRange uxpId="./src/formrange.tsx-1">
    {/* Add meaningful children and properties based on the component's specifications */}
  </FormRange>
);
