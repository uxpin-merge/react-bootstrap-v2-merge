import * as React from 'react';
import Segmented from '../Segmented';

export default (
  <Segmented options={['Daily', 'Weekly', 'Monthly', 'Quarterly', 'Yearly']} defaultValue="Weekly" uxpId="segmented-1" />

);